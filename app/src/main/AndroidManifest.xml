<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.numad21su_jefftaylor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_jt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_jt_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SuJeffTaylor">
        <activity android:name=".Locator"></activity>
        <activity android:name=".LinkCollector" />
        <activity android:name=".ClickyClicky" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>